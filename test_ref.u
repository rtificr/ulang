let f = fn(x: number) {
    x ** 3
}

let step = 0.001

let x1 = 0
let x2 = 5
let cx = x1

let area = 0
while cx < x2 {
    let ta = f(cx) * step
    area = area + ta
    cx = cx + step
}

print(area)