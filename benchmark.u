let makeState = fn() {
    let v = 0

    {
        next = fn() {
            v = v + 1
            v
        },
        inc = fn(x) {
            v = v + x
            v
        },
        get = v,
        ref = &v
    }
}

let s = makeState()

print(s.inc(5))
print(s.get)
